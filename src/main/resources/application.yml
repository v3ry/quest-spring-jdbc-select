spring:
    datasource:
        url: jdbc:h2:file:./demo;SCHEMA=spring_jdbc_quest;CASE_INSENSITIVE_IDENTIFIERS=TRUE
        username: sa
        password: 
        # driverClassName: org.h2.Driver

    # jpa: 
    #     database-platform: org.hibernate.dialect.H2Dialect

    h2:
        console:
            enabled: true
            path: /h2
